Place your trained model files here after running the Colab notebook:

  dermai_model_full.pth   ← trained EfficientNet-B4 weights
  model_meta.json         ← class metadata

Both files are downloaded automatically at the end of the Colab training notebook.
